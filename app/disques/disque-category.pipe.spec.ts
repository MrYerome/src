import { DisqueCategoryPipe } from './disque-category.pipe';

describe('DisqueCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new DisqueCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
